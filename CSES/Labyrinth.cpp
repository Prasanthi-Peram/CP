#include <bits/stdc++.h>
using namespace std;
vector<string> a;
using state = pair<int,int>;
#define rep(i,a,b) for(int i=a;i<b;i++)
//This problem had graph and a different application of BFS: Implicit graph

int main(){
  int n,m;
  cin>>n>>m;
  a.resize(n);
  for(int i=0;i<n;i++) cin>>a[i];

  state st,en;
  rep(i,0,n){
    
  }
  
  return 0;
}
